<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Reversi</title>
  <link rel="stylesheet" href="/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <script>
    function move(id) {
      var tmp = id.split('_')
      var x = tmp[1]
      var y = tmp[2]
      var turn = document.getElementById('turn').value
      location.href = '/move?x=' + x + '&y=' + y + '&color=' + turn
    }
  </script>
</head>

<body>
  <input type="hidden" id="turn" value="<%= @board.data.turn %>">

  <header class="container">
    <h1><a href="/reset">オセロ</a></h1>
    <div class="pull-right">ターン: <strong><%= @board.data.turn %></strong></div>
  </header>

  <div class="container center">
    <table>
      <thead>
        <tr>
          <% (0..8).each do |i| %>
            <th><%= i > 0 ? i.to_s : nil %></th>
          <% end %>
        </tr>
      </thead>
      <tbody>
        <% (1..8).each do |y| %>
          <tr>
            <th><%= y.to_s %></th>
            <% @board.row(y).each_value do |cell| %>
              <td class="cell" id="cell_<%= cell.index %>" onclick="move(id)"><%= cell.draw %></td>
            <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <footer class="container center">
    <button class="btn btn-default" onclick="location.href='/reset'">リセット</button>

    <p><%= @error if @error %></p>

    <% if @request.dev? %>
    <ul>
      <% request.inspect.split(', "').each do |val| %>
        <li><%= val.inspect %></li>
      <% end %>
    </ul>
    <% end %>
  </footer>

</body>
</html>
