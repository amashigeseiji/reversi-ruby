<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Reversi</title>
  <link rel="stylesheet" href="/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <script>
    function move(id) {
      var tmp = id.split('_')
      var x = tmp[1]
      var y = tmp[2]
      location.href = '/move?x=' + x + '&y=' + y
    }
  </script>
</head>

<body>
  <header class="container">
    <h1><a href="/reset">オセロ</a></h1>
    <div class="pull-right">ターン: <strong><%= @board.data.turn %></strong></div>
  </header>

  <div class="container center">

    <table>
      <tbody>
        <% (1..8).each do |y| %>
          <tr>
            <% @board.row(y).each_value do |cell| %>
              <td class="cell" id="cell_<%= cell.index %>" onclick="move(id)"><%= cell.draw %></td>
            <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <footer class="container center">

    <div>
      <% @moves.each do |index, move| %>
        <button class="btn btn-default move" id="move_<%= index %>" onclick="move(id)"><%= index %></button>
      <% end %>
    </div>

    <div>
      <button class="btn btn-default btn-alert" onclick="location.href='/reset'">リセット</button>
    </div>

    <p><%= @error if @error %></p>

    <% if @request.dev? %>
    <ul>
      <% request.inspect.split(', "').each do |val| %>
        <li><%= val.inspect %></li>
      <% end %>
    </ul>
    <% end %>
  </footer>

</body>
</html>
